<Page
    x:Class="BlackJack.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlackJack"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBox Name="email" PlaceholderText="Email"/>
        </Grid>
        <Grid Grid.Row="1">
            <PasswordBox Name="password" PlaceholderText="Password"/>
        </Grid>
        <Grid Grid.Row="2">
            <Button Name="submit" Content="Envoyer"/>
        </Grid>
        <Grid Grid.Row="3">
            <Button Content="register" Click="go_register"></Button>
        </Grid>
    </Grid>
</Page>
