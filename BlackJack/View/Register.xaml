<Page
    x:Class="BlackJack.View.Register"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlackJack.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBox PlaceholderText="username" Name="username" Text="{Binding UpdateSourceTrigger=PropertyChanged, Path=UserName}"></TextBox>
        </Grid>
        <Grid Grid.Row="1">
            <TextBox PlaceholderText="firstname" Name="firstname"></TextBox>
        </Grid>
        <Grid Grid.Row="2">
            <TextBox PlaceholderText="lastname" Name="lastname"></TextBox>
        </Grid>
        <Grid Grid.Row="3">
            <TextBox PlaceholderText="email" Name="email"></TextBox>
        </Grid>
        <Grid Grid.Row="4">
            <PasswordBox PlaceholderText="password" Name="password"></PasswordBox>
        </Grid>
        <Grid Grid.Row="5">
            <PasswordBox PlaceholderText="Repeate password" Name="rpassword"></PasswordBox>
        </Grid>
        <Grid Grid.Row="6">
            <Button Content="Submit"  Command="{Binding Submit}"/>
        </Grid>
        
    </Grid>
</Page>
