<Page
    x:Class="BlackJack.View.GameView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlackJack.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <!--<Button  Content="miser" HorizontalAlignment="Left"  VerticalAlignment="top" Command="{Binding Path=BetCommand}"/>-->
        <Button  Content="nouvelle carte" HorizontalAlignment="center"  VerticalAlignment="Center" Command="{Binding Path=NewCardCommand}"/>
        <Button  Content="stop" HorizontalAlignment="Right"  VerticalAlignment="Center" Command="{Binding Path=StopGameCommand}"/>
        <Slider Name="miser" HorizontalAlignment="Left"  Value="{Binding Path= MyUser.Bet, Mode=TwoWay}" Minimum="2" Maximum="100" TickPlacement="BottomRight" TickFrequency="2" VerticalAlignment="Center"/>
    </Grid>
</Page>
