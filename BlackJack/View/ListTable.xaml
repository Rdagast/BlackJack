<Page
    x:Class="BlackJack.View.ListTable"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlackJack.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <ListView>
                <ListViewItem Margin="0,10,0,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontSize="16" >
                                        <Run Text="{Binding Path=ListTable[0].Seats_available}" />
                                        <!--<Run Text="{Binding Comments[0].Author.Lastname}" />-->
                            </TextBlock>
                            <TextBlock Text=" - " FontSize="12" Margin="2,0,2,0" />
                            <TextBlock Text="{Binding ListTable[0].Created_at}" FontSize="16" />
                        </StackPanel>
                        <!--<TextBlock TextWrapping="Wrap" Text="{Binding Comments[0].Content}" />-->
                    </StackPanel>
                </ListViewItem>
            </ListView>
        </Grid>
    </Grid>
</Page>
