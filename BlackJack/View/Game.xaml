<Page
    x:Class="BlackJack.View.Game"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlackJack.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button x:Name="miser" Content="miser" HorizontalAlignment="Left"  VerticalAlignment="Center" Command="{Binding BetCommand}"/>
        <Button x:Name="carte suivante" Content="carte suivante" HorizontalAlignment="center"  VerticalAlignment="Center" Command="{Binding NextCardCommand}"/>
        <Button x:Name="stop" Content="stop" HorizontalAlignment="Right"  VerticalAlignment="Center" Command="{Binding StopCommand}"/>
    </Grid>
</Page>
